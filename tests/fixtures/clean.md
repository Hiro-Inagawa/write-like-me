# Results

The approach reduces processing time because it removes the preprocessing step that previously took hours, and the saving holds across every dataset we measured, which is the whole reason the team adopted it.

Accuracy rose from 71% to 89% on the held-out set, while the false alarm rate stayed under 2%, so the gain did not come from a looser threshold.
